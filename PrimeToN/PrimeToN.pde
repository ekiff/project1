
// define
float n = 200; //  number inserted
float i; // number of primes
float p = i; // number printed


// for loop
void draw() {
  for (float i = 2; i <= n ; i = i + 1) { // number of primes 2 through n
  
p = i; // redefine variables
   

    
    
// if statements
if (( (p % 2) == 0)|| ((p % 3) == 0) || (p % 5) == 0 || (p % 7) == 0 || (p % 11) == 0) {
}  else {
   println ( p ); 
}

//p % 2) == 0) || ((p % 3) == 0) ||
//(sq(p)*i+1 > n))
//if  {
//}  else {
 //  println (p);
//}
  
//if ; {
  
//println ( p );
}
  




  
 // }  
  }