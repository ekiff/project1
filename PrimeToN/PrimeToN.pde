// set up
void setup() {
}

// define
int n;
int p;

// for loop
void draw() {
  for (int i = n; i <=  ; i = i + 1) {
    
// if statement
if (p <= 1);
if (p / 2);
if (p / 3);
if (6p + 1);

// function
void isPrime(
    
// print sequence
print( p );
 
    
  
  }}